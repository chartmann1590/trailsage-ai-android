# Contributing

Use JDK 17 and Android SDK 36. Run `.\gradlew.bat testDebugUnitTest assembleDebug` before opening a pull request. Do not commit private tokens, `google-services.json`, model binaries, neural voices, or generated map extracts.

