# Firebase Setup
The dedicated Firebase project is `trailsage-ai-android-2026`. Its Android app is registered as `com.charles.trailsage`. Download `google-services.json` into `app/` and keep it untracked. Enable Analytics, Crashlytics, Performance, Remote Config, and FCM only after adding explicit telemetry consent behavior. Debug CI intentionally builds without Firebase config.
