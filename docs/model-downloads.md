# Model Downloads
Manifests live in `app/src/main/assets/manifests`. Download to private storage, resume with HTTP Range, verify SHA-256, and never silently skip required assets. Respect Wi-Fi-only configuration in production scheduling constraints.
