# Tour-Pack Builder
Run `python builder.py "Adirondack Park" --bbox=-74.3,43.9,-73.5,44.5 --out adirondacks`. It uses MediaWiki with a polite user agent and cache. Supply local OSM-derived data with `--osm` for production enrichment; no paid API or API key is used.

